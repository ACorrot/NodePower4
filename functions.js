/* 

const arr = [1, 2, 3]
arr.push(4)
arr.unshift(0)
arr[1] = 'a'
const result = arr.map(value = value * 2) -----> parcours l'array de 2 en 2 par ex



module.exports = {
    write: function write(string) {
        var i = 1
        process.stdout.write(string)
    }
}


process.stdin.on('data', function(chunk) {
    console.log(chunk)
})


*/

/*
Bien lancer le npm install dans un container, pour ne pas avoir de soucis de versions
npm init pour créer un package.json


function mafunc(msg){
    return 'mafunc: ' + msg
}

var func2 = function(){
    return 'func2'
}

var func3 = (msg) => {
    return 'f3 : ' + msg
}

var func4 = asg => 'f4: ' + msg

console.log(mafunc('x'))
//func2()
console.log(func3('x'))


function ask(question){
    rl.question(question, onAnswer)
    
    function onAnswer(answer) {
        console.log(answer)
        ask(question)
    }
}

*/